# jiraclone/frontend/nginx.conf

server {
    listen 80;
    
    # Where Nginx looks for files
    root /usr/share/nginx/html;
    index index.html;

    # The magic line that fixes the 404 error
    location / {
        try_files $uri $uri/ /index.html;
    }
}